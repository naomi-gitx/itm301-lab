apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: sictedu7
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30543
  selector:
    app: frontend
